#ifndef PIPES_COMMON_H       // ifdef guard to protect header from being included multiple times
#define PIPES_COMMON_H

// common name for pipe
#define PIPE_NAME "single_pipe"

// common struct for use in the pipeline
struct example {
  int x;
  float y;
};

#endif //PIPES_COMMON_H

#ifndef PIPELINE_COMMON_H       // ifdef guard to protect header from being included multiple times
#define PIPELINE_COMMON_H

// common name for pipe
#define PIPE_NAME "pipeline_pipe"

// common struct for use in the pipeline
struct example {
  int x;
  float y;
} ;

#endif //PIPELINE_COMMON_H
